# Notebook-derived workflows

The original material contained separate notebooks for classical supervised models, Random Forest/XGBoost ensembles, clustering and anomaly detection. The portfolio edition keeps these concerns in separate modules and reconnects them in `financial_ml_workbench.ipynb`.

This structure makes the breadth of the original work visible without duplicating implementation across notebooks.
